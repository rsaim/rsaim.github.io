# Copy this to .env and fill in real values. .env is git-ignored — never commit it.
# Automatically created for you (from this file) the first time you run
# npm start / npm run build / npm test, via scripts/ensure-local-config.js.

# Static SEO strings injected into public/index.html at build time.
REACT_APP_SITE_TITLE=Your Name | Portfolio
REACT_APP_SITE_DESCRIPTION=Personal portfolio built with React.

# Canonical site URL (no trailing slash) — used for the canonical link tag
# and og:url/og:image/twitter:image. Also update public/sitemap.xml and
# public/robots.txt's Sitemap: line if you fork this to a different domain,
# same as CNAME/homepage.
REACT_APP_SITE_URL=https://your-domain.example.com

# Cloudflare Web Analytics (https://www.cloudflare.com/web-analytics/) —
# free, cookieless, no consent banner needed. Get a token by adding your
# site at https://dash.cloudflare.com/?to=/:account/web-analytics (works
# independent of whether your DNS is on Cloudflare). Leave blank to disable.
REACT_APP_CF_BEACON_TOKEN=

# EmailJS (https://www.emailjs.com/) — powers the contact form.
# See CONTACT_IMPLEMENTATION_GUIDE.md for setup steps.
REACT_APP_EMAILJS_SERVICE_ID=your_service_id_here
REACT_APP_EMAILJS_TEMPLATE_ID=your_template_id_here
REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key_here
